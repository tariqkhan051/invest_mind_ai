# Testing environment configuration

environment: testing
debug: false

database:
  echo: false
  pool_size: 1

api:
  cors_origins: []

scheduler:
  enabled: false
  timezone: "Asia/Karachi"

logging:
  level: WARNING
  rotation: "1 MB"
  retention: "1 day"
